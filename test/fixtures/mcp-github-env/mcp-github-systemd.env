# Test fixture standing in for a filled-in
# ~/.config/mcpod/mcp-github-systemd.env. mcp-github-stdio sources this
# file directly, so GITHUB_APP_PRIVATE_KEY_PODMAN_SECRET here is the one
# value the script actually reads out of it.
GITHUB_APP_PRIVATE_KEY_PODMAN_SECRET="test-github-app-private-key"
