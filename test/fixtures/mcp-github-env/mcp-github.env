# Test fixture standing in for a filled-in ~/.config/mcpod/mcp-github.env.
# Content doesn't matter to mcp-github-stdio beyond being a file it can
# pass to `podman run --env-file=` -- it never reads these values itself.
GITHUB_TOOLSETS=repos,issues,pull_requests
GITHUB_READ_ONLY=1
